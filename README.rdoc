== after cloned in local

$ bundle install

$ rake db:create

$ rake db:install

$ rails s -p 3000

visit 
localhost:3000/cms
username : cmsadmin
password :cmsadmin

then visit 
localhost:3000/cms/sitemap